# JuggleSense セキュリティ・バグ点検指示書

以下の観点でコードベース全体をレビューし、問題があれば修正してください。
各カテゴリごとに「発見した問題」「修正内容」を記録し、最後にまとめて報告してください。
修正後は `npm run build` が通ることを確認し、commit & push してください。

---

## 1. セキュリティ

- リポジトリ全体(コード・コミット履歴・`.env`系ファイル)に、APIキー・トークン・パスワード・個人情報等のハードコードされた秘匿情報が存在しないか確認する。見つかった場合は除去し、もし既にpush済みであれば該当キーは無効化が必要である旨を報告する
- `npm audit` を実行し、High/Critical判定の脆弱性がある依存パッケージがあれば、可能な範囲で安全なバージョンに更新する(メジャーバージョンアップで破壊的変更が伴う場合は更新せず一覧化して報告のみとする)
- `dangerouslySetInnerHTML` やユーザー入力を直接DOMやテンプレートに埋め込んでいる箇所がないか確認する(本ツールは数値入力のみのフォームだが念のため)
- `next.config.js` 等に不要に緩いCORS設定や、外部への意図しないリクエストが発生する設定がないか確認する

## 2. ルーティング・存在しないページ

- `/[machineId]` および `/[machineId]/reverse` に、データJSONに存在しない`machineId`(例: `/foo`)がアクセスされた場合、クラッシュせずNext.jsの404ページ(`notFound()`)を返すようにする
- 上記が未対応であれば `app/[machineId]/page.tsx` / `app/[machineId]/reverse/page.tsx` に存在チェックと `notFound()` 呼び出しを追加する

## 3. 数値計算のNaN/Infinity/負数

- `lib/judge.ts` と `lib/reverseCalc.ts` 全体を再点検し、以下のパターンで `NaN` / `Infinity` / `-Infinity` がUIにそのまま表示される箇所がないか確認する
  - 0除算(分母が0または未定義)
  - `Math.log(0)` や `Math.log(負数)`
  - 配列が空の状態での `Math.max(...[])` 等
- 発見した箇所は、計算前にガード処理を入れるか、結果が異常値の場合は適切なフォールバック表示(「計算不能」「データ不足」等)に置き換える

## 4. 型安全性

- `any` 型が使われている箇所を確認し、可能であれば具体的な型に置き換える。型推論が難しい場合のみ `unknown` + 型ガードで対応する
- `tsconfig.json` の `strict` モードが有効になっているか確認し、無効であれば有効化した上でビルドエラーが出ないか確認する(エラーが多数発生し対応コストが大きい場合は、有効化はせず現状の`any`使用箇所のリストアップのみ報告する)

## 5. レスポンシブ・UI崩れ

- 画面幅375px(iPhone SE相当)を想定し、各ページ(機種選択・判別・逆算)で以下が崩れないか確認する
  - フォーム入力欄が画面幅を超えてオーバーフローしていないか
  - 機種カードのバッジ・テキストが折り返しで重なっていないか
  - 「設定別ぶどう確率との比較」テーブルが横スクロールなしで読めるか
- 問題があれば Tailwind のレスポンシブクラス(`sm:` `md:` 等)で調整する

---

## 完了条件

- 上記1〜5それぞれについて「発見事項」と「対応内容(対応不要なら理由)」を箇条書きでまとめる
- `npm run build` が成功する
- 修正がある場合は commit & push する
